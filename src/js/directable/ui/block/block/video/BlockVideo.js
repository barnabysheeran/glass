import Block from '../Block';

export default class BlockVideo extends Block {
	// _________________________________________________________________________

	constructor(container) {
		super(container, 6, 6, 2);
	}
}
